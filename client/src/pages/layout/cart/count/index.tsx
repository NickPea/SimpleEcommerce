//

import React, { useState } from 'react';
import styled from 'styled-components';


interface Proptypes {
	start: number;
}
const Count = ({start}: Proptypes) => {
	const [_count, set_count] = useState(start);
	return (
		
        
	);
};